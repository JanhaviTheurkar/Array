#include <stdio.h>
int main() {
    int a[10] = {10, 20, 30, 40, 50};
    int n = 5, pos = 3;  // delete 3rd element
    for(int i = pos - 1; i < n - 1; i++)
        a[i] = a[i + 1];
    n--;
    printf("After deletion: ");
    for(int i = 0; i < n; i++) printf("%d ", a[i]);
}
